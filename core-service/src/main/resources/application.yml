spring:
  application:
    name: core-service
  datasource:
    url: jdbc:postgresql://localhost:5432/forms
    password: forms
    username: forms
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

server:
  port: 8080